// import { page, actions, before, close } from "_helpers/test.utils";

// const { action, selector } = actions;

// const stars = async () =>
//   await page.evaluate(() => {
//     const star = page.querySelector(".star");
//   });

// before("http://localhost:3000");

// test("active comment modal", async () => {
//   await selector(".stars");
//   await page.click('span');
 
//   //await action(".comment__title", "Add new Comment");
// },100000);
