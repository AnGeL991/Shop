import { FC } from "react";
import { ForgetPasswordForm } from "components/template";

export const ForgetPassword: FC = () => {
  return (
    <section className="page">
      <ForgetPasswordForm />
    </section>
  );
};
